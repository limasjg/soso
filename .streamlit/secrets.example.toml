# Nunca envie secrets.toml para o GitHub. Use este arquivo apenas como modelo.

DATABASE_URL = "postgresql+psycopg2://postgres:SUA_SENHA@db.SEUPROJETO.supabase.co:5432/postgres"

[access]
# O único e-mail Google que poderá usar o SOSO.
allowed_email = "seu-email@gmail.com"

[auth]
# No deploy, use: https://SEU-SUBDOMINIO.streamlit.app/oauth2callback
# Localmente, use: http://localhost:8501/oauth2callback
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = "gere-uma-string-aleatoria-longa-e-secreta"
client_id = "SEU_CLIENT_ID.apps.googleusercontent.com"
client_secret = "SEU_CLIENT_SECRET"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
